##
##  config.layout -- Pre-defined Installation Path Layouts
##

#   Classical OpenSSL path layout.
<Layout OpenSSL>
    ssl_prefix:   /usr/local
    ssl_dir:   ${ssl_prefix}/ssl
    ssl_libdir: lib
</Layout>
